import { Connection, Keypair, PublicKey } from "@solana/web3.js";
import * as anchor from "@coral-xyz/anchor";
import { UserAccounts, GlobalAccounts } from "./utils";
import { Cluster } from "./utils";
import Decimal from "decimal.js";
export type Env = {
    provider: anchor.AnchorProvider;
    conn: Connection;
    program: anchor.Program;
    admin: Keypair;
    cluster: Cluster;
};
export declare function setUpProgram(args: {
    clusterOverride?: string;
    adminFilePath?: string;
    programOverride?: PublicKey;
}): Env;
export declare function createGlobalAccounts(env: Env, owner?: Keypair, tokenSymbols?: string[]): Promise<GlobalAccounts>;
export declare function setGlobalAccounts(env: Env, owner?: Keypair, skipInitVaults?: boolean): Promise<GlobalAccounts>;
export declare function createUser(env: Env, globalAccounts: GlobalAccounts, tokenAndAirdropAmounts: {
    token: string;
    amount: Decimal;
}[], owner?: Keypair): Promise<UserAccounts>;
//# sourceMappingURL=setup.d.ts.map